node_modules/
dist/
test-3years.mjs
node_modules/

# Capturas de pantalla de las pruebas — material temporal, no del producto
*.png

# SDK de Firebase servido en local durante las pruebas del emulador
__sdk/
test/simulacion/salida/
test/capturas/

# Recorte de la fuente de iconos: lo genera build.sh desde
# css/tabler-icons.min.css, no se commitea (como dist/).
build/

# Las creatividades y capturas se generan con anuncios/creatividades.mjs;
# se suben a Meta, no al repositorio (como dist/ y build/).
anuncios/salida/
anuncios/capturas/
